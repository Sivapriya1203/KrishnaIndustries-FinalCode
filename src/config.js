const config = {
  // apiUrl: `https://backend.krishnaindustries.cloud`,
  apiUrl: `http://localhost:2002`,
  basename: '',
  defaultPath: '/dashboard/default',
  fontFamily: `'Roboto', sans-serif`,
  borderRadius: 12
};

export default config;

